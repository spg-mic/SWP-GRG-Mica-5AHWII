datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client"
    output   = "client"
    runtime  = "deno"
}

model Airport {
    id       String @id @default(uuid())
    name     String
    iataCode String @unique
    city     String
}
